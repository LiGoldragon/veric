// veric — aski verifier + linker. Placeholder; full rewrite pending.
//
// Previous implementation (pre-v0.18) deleted 2026-04-20 after it
// accumulated 79 compile errors against current aski-core. Design
// notes preserved in CLAUDE.md §Verification Architecture.
//
// Blocked on: askic-assemble crate + askic producing real per-module
// rkyv output. See CLAUDE.md §How to update.

fn main() {
    eprintln!("veric: not yet implemented (awaiting askic-assemble + askic rewrite)");
    std::process::exit(1);
}
